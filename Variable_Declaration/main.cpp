#include <iostream>

int main()
{
	bool boolean; //Can be initialized to true/false or 1/0;
	char character; //Usually initialized to an ASCII character (which cooresponds to an integer); 

	int integer; //Can be initialized to integer value within -(2^32)->(2^32)-1;
	short int shortInteger; //Can be intitialized to an integer value within -(2^16)->(2^16)-1;
	long int longInteger; //Can be initialized to an integer value withing -(2^32)->(2^32)-1;
	long long int longLongInteger; //Can be intialized to an integer value within -(2^64)->(2^64)-1;
	
	float numFloat;

	double numDouble;
	long double numLongDouble;

	return 0;
}